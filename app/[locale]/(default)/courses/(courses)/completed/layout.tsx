export const metadata = {
  title: "Completed Courses",
  description: "Completed Courses",
};

export default function CompletedCoursesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
