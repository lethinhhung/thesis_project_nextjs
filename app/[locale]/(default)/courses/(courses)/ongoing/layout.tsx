export const metadata = {
  title: "Ongoing Courses",
  description: "Ongoing courses page",
};

export default function OngoingCoursesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
